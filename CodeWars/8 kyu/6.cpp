/*
* Напишите функцию, которая принимает массив чисел и возвращает их сумму. 
* Числа могут быть отрицательными или нецелочисленными.
*  Если в массиве нет чисел, функция должна вернуть 0.
*/

#include <iostream>
#include <vector>

using namespace std;

int main()
{
    const int n = 5;

    int array[n];

    for (int i = 0; i < n; i++)
    {
        cin >> array[i];
    }
    
}

